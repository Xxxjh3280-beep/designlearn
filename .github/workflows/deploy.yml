name: Deploy to GitHub Pages

on:
  push:
    # 当推送到 main 分支时触发部署
    branches:
      - main
  # 允许手动从 Actions 标签页触发部署
  workflow_dispatch:

# 设置权限，部署到 GitHub Pages 需要
permissions:
  contents: read
  pages: write
  id-token: write

# 取消所有先前运行的作业
concurrency:
  cancel-in-progress: true
  group: pages

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: ⬇️ Checkout 仓库代码
        uses: actions/checkout@v4
        
      - name: ⚙️ 设置 pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 8 
          run_install: false # 仅设置 pnpm，不运行 install
          
      - name: 🟢 设置 Node.js 环境
        uses: actions/setup-node@v4
        with:
          node-version: 20 # 推荐使用 LTS 版本
          cache: 'pnpm' # 缓存 pnpm 依赖
          
      - name: 📦 安装依赖
        run: pnpm install
        
      - name: 🛠️ 构建项目
        run: pnpm run build
        
      # 注意：Vite 构建默认输出到 'dist' 目录
      - name: 🚀 部署到 GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          # 使用 GitHub Token 进行认证
          github_token: ${{ secrets.GITHUB_TOKEN }}
          # 要部署的构建目录 (默认是 dist)
          publish_dir: ./dist
          # 部署分支，通常使用 gh-pages
          publish_branch: gh-pages
          # 提交信息
          commit_message: "Deploy to GitHub Pages from ${{ github.ref }}"